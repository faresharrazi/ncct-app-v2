<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <% if user_signed_in? %>
      <!-- When user is signed in, NCCT links to the dashboard -->
      <%= link_to "NCCT", main_account_path(current_user.main_accounts.first), class: "navbar-brand fs-3 fw-bold" %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" 
              aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <!-- Subaccounts -->
          <li class="nav-item">
            <%= link_to "Accounts", 
                        main_account_sub_accounts_path(current_user.main_accounts.first), 
                        class: "nav-link #{'active' if current_page?(main_account_sub_accounts_path(current_user.main_accounts.first))}" %>
          </li>

          <!-- Transactions -->
          <% if current_user.main_accounts.any? %>
            <li class="nav-item">
              <%= link_to "Main Transactions", 
                          main_account_main_transactions_path(current_user.main_accounts.first), 
                          class: "nav-link #{'active' if current_page?(main_account_main_transactions_path(current_user.main_accounts.first))}" %>
            </li>
            <li class="nav-item">
              <%= link_to "SubAccount Transactions", 
                          main_account_sub_accounts_path(current_user.main_accounts.first), 
                          class: "nav-link #{'active' if current_page?(main_account_sub_accounts_path(current_user.main_accounts.first))}" %>
            </li>
          <% end %>
        </ul>

        <!-- Profile Dropdown -->
        <ul class="navbar-nav mb-2 mb-lg-0">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle #{'active' if current_page?(edit_user_registration_path(current_user)) || current_page?(main_account_shared_main_account_users_path(current_user.main_accounts.first))}" 
               href="#" id="profileDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <i class="fas fa-user"></i> <%= current_user.full_name %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="profileDropdown">
              <li>
                <%= link_to "Settings", 
                            edit_user_registration_path(current_user), 
                            class: "dropdown-item #{'active' if current_page?(edit_user_registration_path(current_user))}" %>
              </li>
              <% if current_user.main_accounts.any? %>
                <li>
                  <%= link_to "Partners", 
                              main_account_shared_main_account_users_path(current_user.main_accounts.first), 
                              class: "dropdown-item #{'active' if current_page?(main_account_shared_main_account_users_path(current_user.main_accounts.first))}" %>
                </li>
              <% end %>
              <li>
                <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
              </li>
            </ul>
          </li>
        </ul>
      </div>
    <% else %>
      <!-- When user is not signed in, NCCT is plain text -->
      <div class="w-100 text-center">
        <span class="navbar-brand fs-3 fw-bold">NCCT</span>
      </div>
    <% end %>
  </div>
</nav>
