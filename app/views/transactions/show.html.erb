<div class="container mt-4">
  <div class="card shadow-sm" style="max-width: 600px; margin: 0 auto;">
    <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
      <h1 class="h5 mb-0"><%= @transaction.title %></h1>
      <small><%= @transaction.created_at.strftime('%B %d, %Y - %H:%M') %></small>
    </div>
    <div class="card-body">
      <!-- Description -->
      <p>
        <strong>Description:</strong>
        <span class="text-muted"><%= @transaction.description.presence || "No description provided" %></span>
      </p>

      <!-- Amount -->
      <p>
        <strong>Amount:</strong>
        <span class="<%= @transaction.transaction_kind == 'expense' ? 'text-danger' : 'text-success' %>">
          <%= number_to_currency(@transaction.amount, unit: @transaction.main_account&.currency || @transaction.sub_account&.main_account&.currency || "€") %>
        </span>
      </p>

      <!-- Type -->
      <p>
        <strong>Type:</strong>
        <%= @transaction.transaction_kind.capitalize %>
      </p>

      <!-- Category -->
      <p>
        <strong>Category:</strong>
        <span class="text-muted"><%= @transaction.category&.title || "Uncategorized" %></span>
      </p>

      <!-- Creator -->
      <p>
        <strong>Creator:</strong>
        <span class="text-muted"><%= @transaction.creator.first_name %> <%= @transaction.creator.last_name %></span>
      </p>
    </div>
    <div class="card-footer d-flex justify-content-between">
      <%= link_to "Edit", edit_transaction_path(@transaction), class: "btn btn-primary btn-sm" %>
      <%= link_to "Back", transactions_path, class: "btn btn-secondary btn-sm" %>
    </div>
  </div>
</div>
